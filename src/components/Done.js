import React, { Component } from 'react';
import { Container } from '@material-ui/core'

class Done extends Component {
    render() {
        return (
            <Container>
                done
            </Container>
        )
    }
}

export default Done;